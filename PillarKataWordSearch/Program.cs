using System;
using System.IO;

namespace PillarKataWordSearch
{
    class Program
    {
        static void Main(string[] args)
        {
            var file = args[0];
            using (var stream = new StreamReader(file))
            {

            }

            Console.ReadLine();
        }
    }
}
